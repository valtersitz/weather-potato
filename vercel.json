{
    "buildCommand": "cd PWApp && npm install && npm run build",
    "outputDirectory": "PWApp/dist",
    "framework": "vite",
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/index.html"
      }
    ]
  }